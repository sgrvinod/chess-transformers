"""Model configuration definitions.

This package contains configuration files for various Chess Transformer models,
defining architecture hyperparameters like model dimensions, layer counts,
and vocabulary sizes.

Key Features:
    - base: ModelConfig, TrainConfig, and DataLoaderConfig base classes.
    - vole: Configuration for the Vole model (512-dim, 6 layers).

Notes:
    Use the `import_config` utility function from `chess_transformers.utilities.configs`
    to load configurations by name rather than importing directly.
"""
